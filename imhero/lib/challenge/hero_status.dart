import 'package:flutter/material.dart';

class HeroStatus extends StatelessWidget {
  const HeroStatus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
